services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=Password9527
    ports:
      - 3306:3306
  
  adminer: # 数据库管理工具
    image: adminer
    restart: always
    ports:
      - 8080:8080
